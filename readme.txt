In order to run this program, enter the following commands in the terminal:

python -m venv myenv

.\myenv\Scripts\activate

pip install language-tool-python

pip install SpeechRecognition pyaudio nltk gensim transformers

after completing library downloads pwede nyo na irun <33